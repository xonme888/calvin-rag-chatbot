# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.ruff_cache/

# Virtual environment
.venv/
venv/

# Environment
.env
.env.bak
.env.local

# 데이터 — 칼빈 PDF (저작권). 글로서리 JSON 만 별도 COPY.
data/calvin/
*.pdf
*.epub

# 인덱스 — 본문 청크가 들어 있어 저작권 자산. image 에 포함 금지.
# 운영: 부팅 시 Private HF Dataset 에서 fetch (scripts/boot.sh)
indexes/
*.faiss
*.pkl

# Docs / Tests / Web (분리 배포)
# README.md 는 Dockerfile 의 `COPY README.md ./` 에 필요하므로 제외 X.
# (HF Space frontmatter + pyproject 메타데이터에서 사용)
docs/
tests/
web/
.github/

# Git
.git/
.gitignore

# IDE
.vscode/
.idea/
.DS_Store
