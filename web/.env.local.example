# Calvin RAG Chatbot — Next.js 프론트 환경변수
# 이 파일을 .env.local 로 복사:
#   cp .env.local.example .env.local

# FastAPI 백엔드 URL (개발 시 localhost:8000)
NEXT_PUBLIC_API_BASE=http://localhost:8000

# 운영 단계 (Phase 2 Step 6 배포 시):
# NEXT_PUBLIC_API_BASE=https://api.calvin-rag.example.com
